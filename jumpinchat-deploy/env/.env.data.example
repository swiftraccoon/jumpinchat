# JumpInChat — Data server environment
# For multi-server: copy to .env on the database server

# MongoDB runs without auth by default (Docker network isolation).
# For multi-server, enable authentication:
# MONGODB_USER=jicapp
# MONGODB_PASS=

# Redis runs without auth by default.
# For multi-server, set a password:
# REDIS_PASSWORD=
